# Task Implementation Report

**Change**: mg-293
**Task ID**: T4_3
**Task Title**: Repoint must_gather_output to cleaned staging directory
**Phase**: Phase 4 — Upload pipeline hook
**Assigned Agent**: upload-script
**Approved**: 2026-07-22T14:54:00+05:30
**User approved by**: user confirmation

## Summary

After successful obfuscation, the upload script repoints `must_gather_output` to `${must_gather_upload}/cleaned` so tar/SFTP archive the cleaned tree only, with a fail-fast check for missing or empty output.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `build/bin/upload` | Assign `must_gather_output` to cleaned staging path; validate non-empty directory before archive |

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| `obfuscate=true` tar uses cleaned path | PASSED | Archive line shows `…/cleaned` |
| Obfuscation disabled unchanged | PASSED | Archive line shows raw input path |
| Empty cleaned dir fails fast | PASSED | Exit 1 with clear error |
| `bash -n build/bin/upload` | PASSED | Syntax valid |

## Deviations

None.
