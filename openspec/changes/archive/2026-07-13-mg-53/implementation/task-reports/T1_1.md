# Task Implementation Report

**Change**: mg-53
**Task ID**: T1_1
**Task Title**: Define uploadTarget API types and CEL validation
**Phase**: Phase 1 — API schema
**Assigned Agent**: API_Agent
**Approved**: 2026-07-13T04:21:00Z
**User approved by**: user confirmation

## Summary

Introduced the `uploadTarget` discriminated union on `MustGatherSpec` with SFTP upload configuration, CEL validation, and removal of legacy top-level upload fields. API package builds and vets cleanly. Controller and test migration deferred to downstream tasks.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `api/v1alpha1/mustgather_types.go` | Added `UploadTarget`, `SFTPUploadTargetConfig`, `UploadType`; CEL union rule; removed legacy `caseID`, `caseManagementAccountSecretRef`, `internalUser` |

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria | PASSED | Types compile; CEL enforces SFTP requires sftp block |
| `go build ./api/...` | PASSED | |
| `go vet ./api/...` | PASSED | |

## Deviations

None
